#ifndef ENUMS_H
#define ENUMS_H

enum DATA_SOURCE
{
	FILE_PATH,
	STR_DATA
};
enum SH_TYPE
{
	VERTEX,
	FRAGMENT,
	GEOMETRY
};
enum INTERNAL_EVENT
{
	SYS_START
};
enum TEX_TYPE
{
	TEX_DIFF,
	TEX_SPEC,
	TEX_NORM,
	TEX_HEIGHT,
	TEX_CUBEMAP
};
enum FILTERS
{
	WRAP_S,
	WRAP_R,
	WRAP_T,
	MIN_F,
	MAG_F
};
enum CAMERA_DIR
{
	FORWARD,
	BACKWARD,
	LEFT,
	RIGHT,
	UP,
	DOWN,
};
enum KEY_STATE
{
	IDLE = 0,
	PRESSED = 1,
	RELEASED = 2
};
enum SORT_ORDER
{
	Z_ORDER,
	Y_ORDER
};

#endif // !ENUMS_H